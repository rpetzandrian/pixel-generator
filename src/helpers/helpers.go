package helpers

// func GeneratePixelArray(size int) entity.PixelArray {
// 	r := rand.New(rand.NewSource(time.Now().UnixNano()))
// 	pixels := make(entity.PixelArray, size)

// 	for i := 0; i < size; i++ {
// 		pixels[i] = make([]entity.Pixel, size)
// 		for j := 0; j < size; j++ {
// 			// Assign random values to the pixel
// 			pixels[i][j] = entity.Pixel{
// 				Value: r.Intn(4),  // Random pixel value between 0 and 3
// 				Color: r.Intn(17), // Random color code between 0 and 16 (adjust range as needed)
// 			}
// 		}
// 	}

// 	return pixels
// }
